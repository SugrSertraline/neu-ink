# NeuInk - 云端论文阅读器

NeuInk 是一个云端多用户论文阅读和笔记管理系统，采用简化的 monorepo 架构设计。

## 🚀 项目特点

- **前后端分离**: Python Flask + Next.js TypeScript
- **简化架构**: 清晰的应用层结构，配置就近管理
- **类型安全**: 前后端共享 TypeScript 类型定义
- **模块化设计**: 清晰的代码组织和依赖管理
- **开发体验优化**: 统一的工具链和开发流程

## 📁 项目结构

```
neu-ink-backend/
├── apps/                    # 应用层
│   ├── api/                # Python Flask 后端
│   │   ├── neuink/         # Flask 应用核心
│   │   │   ├── models/     # 数据模型
│   │   │   ├── routes/     # API 路由
│   │   │   ├── services/   # 业务逻辑服务
│   │   │   └── config/     # 后端配置
│   │   ├── requirements.txt
│   │   └── run.py
│   ├── web/                # Next.js 前端
│   │   ├── src/
│   │   │   ├── app/        # App Router 页面
│   │   │   ├── components/ # React 组件
│   │   │   └── lib/        # 工具函数和配置
│   │   │       └── config/ # 前端配置
│   │   └── package.json
│   └── shared/             # 共享代码
│       ├── types/          # TypeScript 类型定义
│       │   ├── api.ts      # API 接口类型
│       │   ├── database.ts # 数据库模型类型
│       │   ├── common.ts   # 通用类型
│       │   └── index.ts    # 类型导出
│       └── package.json
├── docs/                   # 项目文档
├── package.json            # 工作空间配置
└── README.md
```

## 🛠️ 技术栈

### 后端
- **Python 3.9+** - 编程语言
- **Flask** - Web 框架
- **MongoDB** - 数据库
- **PyMongo** - MongoDB 驱动
- **JWT** - 身份验证

### 前端
- **Node.js 18+** - 运行环境
- **Next.js 15** - React 框架
- **TypeScript** - 类型安全
- **Tailwind CSS** - 样式框架
- **shadcn/ui** - UI 组件库

### 开发工具
- **NPM Workspaces** - 包管理
- **Prettier** - 代码格式化
- **ESLint** - 代码检查
- **Concurrently** - 并行脚本执行

## 🚀 快速开始

### 环境要求

- Node.js 18.0.0+
- Python 3.9.0+
- MongoDB 4.4+
- NPM 9.0.0+

### 手动设置

1. **克隆项目并安装根依赖**
   ```bash
   git clone <repository-url>
   cd neu-ink-backend
   npm install
   ```

2. **设置后端环境**
   ```bash
   cd apps/api
   python -m venv .venv
   
   # Windows
   .venv\Scripts\activate
   
   # Linux/macOS
   source .venv/bin/activate
   
   pip install -r requirements.txt
   cd ../..
   ```

3. **设置前端环境**
   ```bash
   cd apps/web
   npm install
   cd ../..
   ```

4. **配置环境变量**
   
   复制并编辑环境变量文件：
   ```bash
   # 后端环境变量
   cp apps/api/.env.example apps/api/.env
   
   # 前端环境变量
   cp apps/web/.env.example apps/web/.env.local
   ```

5. **启动数据库**
   ```bash
   # 确保 MongoDB 正在运行
   mongod
   ```

### 开发命令

```bash
# 启动开发服务器 (前后端同时)
npm run dev

# 仅启动后端
npm run dev:api

# 仅启动前端
npm run dev:web

# 构建项目
npm run build

# 运行测试
npm run test

# 代码检查
npm run lint

# 代码格式化
npm run format

# 类型检查
npm run type-check

# 清理依赖和构建文件
npm run clean
```

## 🌐 服务地址

- **前端应用**: http://localhost:3000
- **后端API**: http://localhost:8000
- **API文档**: http://localhost:8000/docs (计划中)

## 📦 包管理

### 共享类型使用

项目使用 NPM Workspaces 管理包依赖，共享类型可以在前端中复用：

```typescript
// 在前端中使用共享类型
import { User, Paper } from '@neu-ink/shared/types';

// 使用前端配置
import { API_ENDPOINTS, THEME_CONSTANTS } from '@/lib/config';
```

### 添加依赖

```bash
# 添加根依赖
npm install <package> --workspace=root

# 添加前端依赖
npm install <package> --workspace=apps/web

# 添加后端依赖 (在 apps/api 目录下)
cd apps/api && pip install <package>

# 添加共享类型依赖
npm install <package> --workspace=apps/shared
```

## 🔧 开发指南

### 代码组织原则

1. **应用层 (apps/)**: 独立可运行的应用
2. **共享层 (apps/shared/)**: 可复用的类型定义
3. **配置管理**: 每个应用管理自己的配置

### 类型定义管理

- 所有 TypeScript 类型定义在 `apps/shared/types/` 中维护
- API 接口类型与数据库模型类型分离
- 前后端共享相同的数据结构定义

### 配置管理

- 前端配置在 `apps/web/src/lib/config/` 中管理
- 后端配置在 `apps/api/neuink/config/` 中管理
- 配置就近原则，每个应用维护自己的配置

## 📖 API 文档

### 主要端点

- `GET /api/v1/health` - 健康检查
- `POST /api/v1/auth/login` - 用户登录
- `GET /api/v1/papers` - 获取论文列表
- `POST /api/v1/papers` - 上传论文

详细 API 文档请参考 [docs/项目说明文档.md](./docs/项目说明文档.md)。

## 🧪 测试

```bash
# 运行所有测试
npm run test

# 运行后端测试
npm run test:api

# 运行前端测试
npm run test:web
```

## 📋 开发计划

项目按照以下阶段进行开发：

1. **第一阶段**: 基础设施与核心功能 ✅
   - 用户认证系统
   - 公共论文库核心模型
   - 用户论文关系管理

2. **第二阶段**: 高级功能开发 (进行中)
   - 清单系统
   - 笔记系统
   - 公开评论系统

3. **第三阶段**: 管理功能与优化
   - 管理员功能
   - 搜索与浏览功能

4. **第四阶段**: 性能优化与扩展
   - 性能优化
   - 用户体验优化

详细开发计划请参考 [docs/项目说明文档.md](./docs/项目说明文档.md)。

## ✨ 架构优势

- **结构清晰**: 每个应用独立管理自己的配置
- **类型安全**: 前后端共享 TypeScript 类型定义
- **维护性强**: 配置就近原则，更易于维护
- **开发效率**: 简化的项目结构和脚本
- **扩展性好**: 易于添加新的应用和功能

## 🤝 贡献指南

1. Fork 项目
2. 创建功能分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

## 📄 许可证

本项目采用 MIT 许可证。详情请见 [LICENSE](LICENSE) 文件。

## 👥 团队

- **NeuInk Team** - *项目维护* - [GitHub](https://github.com/neuink)

## 🙏 致谢

感谢所有为本项目做出贡献的开发者。

---

**注意**: 这是一个重构后的项目结构，采用简化的应用层架构，配置就近管理，提高了项目的可维护性。